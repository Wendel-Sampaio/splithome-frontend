import { DocheckDirective } from './docheck.directive';

describe('DocheckDirective', () => {
  it('should create an instance', () => {
    const directive = new DocheckDirective();
    expect(directive).toBeTruthy();
  });
});
